<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/theme_color</item>
        <item name="colorPrimaryDark">@color/theme_color</item>
        <item name="colorAccent">@color/theme_color</item>
        <!--        <item name="android:windowBackground">@android:color/transparent</item>-->
        <item name="android:windowIsTranslucent">true</item><!-- 设置Activity透明，解决切换Activity动画的黑屏 -->
        <!--        <item name="android:windowSwipeToDismiss">true</item>&lt;!&ndash; 开启手势侧滑退出Activity &ndash;&gt;-->
        <item name="android:windowAnimationStyle">@style/anim_slide_right</item>
        <item name="android:windowBackground">@color/bg_color</item>
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <style name="Theme.SplashTheme.NewSplash" parent="Theme.SplashScreen">
        <!-- 启动画面背景色 -->
        <item name="windowSplashScreenBackground">@android:color/white</item>
        <!-- 启动画面icon图标：这里可以是图片、帧动画等-->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_small</item>
        <item name="windowSplashScreenAnimationDuration">10</item>
        <!-- Splash退出后的主题-->
        <item name="postSplashScreenTheme">@style/Theme.AppTheme</item>
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
    </style>
</resources>